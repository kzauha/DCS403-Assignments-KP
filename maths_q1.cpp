#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double n = -9;

    cout << "Square root of 9 = " << sqrt(fabs(n)) << endl;
    cout << "2 raised to power 3 = " << pow(2, 3) << endl;
    cout << "Absolute value of -9 = " << fabs(n) << endl;

    return 0;
}
